# REMARK Admission — Verification

| ID | Status | Summary | Verification Approach |
|----|--------|---------|----------------------|
| [DSL-102](NORMATIVE.md#DSL-102) | ✅ | Evidence wire roundtrip | 5 tests against new `src/remark/evidence_wire.rs`: `encode_slashing_evidence_remark_payload_v1(&ev)` emits `SLASH_EVIDENCE_REMARK_MAGIC_V1 \|\| serde_json(ev)`; `parse_slashing_evidence_from_conditions(&[P: AsRef<[u8]>])` strips magic + decodes, silent-skipping short/foreign/malformed/wrong-schema payloads. Roundtrip PartialEq; magic-prefix substring pinned to `b"DIG_SLASH_EVIDENCE_V1\0"`; empty + short-prefix + foreign-prefix + garbage-JSON + wrong-schema-JSON all skip cleanly; interleaved valid+foreign+valid list preserves input order. Opens Phase 6 REMARK Admission with new `src/remark/` module + `SLASH_EVIDENCE_REMARK_MAGIC_V1` / `SLASH_APPEAL_REMARK_MAGIC_V1` constants (appeal prefix landed early so DSL-110 can reuse without a second constants commit). Test file: `tests/dsl_102_evidence_remark_wire_roundtrip_test.rs`. |
| [DSL-103](NORMATIVE.md#DSL-103) | ✅ | Evidence puzzle_reveal emits one REMARK | 4 tests against new `slashing_evidence_remark_puzzle_reveal_v1` + `slashing_evidence_remark_puzzle_hash_v1` in `src/remark/evidence_wire.rs`. Puzzle shape: quoted constant `(q . ((1 payload)))` where outer `1` is the CLVM quote opcode and inner `1 payload` is the REMARK condition in canonical proper-list form. Solution-less by design (prevents post-creation payload substitution). Built via clvmr Allocator + serialised through `node_to_bytes`; hash via `clvm_utils::tree_hash_from_bytes`. `cargo test` runs the puzzle in clvmr `ChiaDialect(0)` via `chia_sdk_types::run_puzzle`: output decodes as `Vec<Condition<NodePtr>>` of length 1 matching `Condition::Remark`; REMARK payload atom extracted via `allocator.sexp` pair-walk round-trips through DSL-102 `parse_slashing_evidence_from_conditions` to the original evidence; puzzle hash stable across repeated calls AND changes when `reporter_validator_index` mutates; hash equals `tree_hash_from_bytes(reveal)`. Adds `clvmr`, `clvm-utils` as main deps + `chia-sdk-types`, `clvm-traits` as dev-deps. Test file: `tests/dsl_103_evidence_puzzle_reveal_emits_one_remark_test.rs`. |
| [DSL-104](NORMATIVE.md#DSL-104) | ✅ | Admission matching coin | 4 tests against new `enforce_slashing_evidence_remark_admission(&SpendBundle, &HashMap<Bytes32, Vec<Vec<u8>>>)` in `src/remark/policy.rs`. For each coin spend: look up coin_id → REMARK payloads (unwrap_or empty), parse via DSL-102 (foreign/malformed silently skip), for each parsed evidence derive DSL-103 puzzle hash, compare with `coin.puzzle_hash`. Mismatch raises new `SlashingError::AdmissionPuzzleHashMismatch { expected: Bytes32, got: Bytes32 }` variant. 4 tests: single matching spend admits, two-spend bundle with both matching admits, empty bundle admits vacuously, spend absent from conditions map admits (no evidences to enforce). Test file: `tests/dsl_104_evidence_admission_matching_coin_test.rs`. |
| [DSL-105](NORMATIVE.md#DSL-105) | ✅ | Admission mismatched rejected | 3 tests against the DSL-104 `enforce_slashing_evidence_remark_admission` fail path. Wrong `coin.puzzle_hash` → `SlashingError::AdmissionPuzzleHashMismatch { expected, got }`; both fields populated with correct values (expected == DSL-103-derived hash, got == coin ph); `format!` render (thiserror Display) contains both 64-char lowercase-hex strings; short-circuit verified by 3-spend bundle (match, mismatch_b, mismatch_c) where error's `got` equals mismatch_b's ph and never mismatch_c's — proves iteration halts at first failure. Test file: `tests/dsl_105_evidence_admission_mismatch_rejected_test.rs`. |
| [DSL-106](NORMATIVE.md#DSL-106) | ✅ | Mempool expired rejected | 5 tests against new `enforce_slashing_evidence_mempool_policy(&SpendBundle, &HashMap, current_epoch: u64)` in `src/remark/policy.rs`. Check: `ev.epoch + SLASH_LOOKBACK_EPOCHS < current_epoch` → `SlashingError::OffenseTooOld { offense_epoch, current_epoch }` (reuses existing DSL-011 variant since the predicate is identical). Addition on LHS prevents underflow at genesis. 5 tests: expired rejected with both epochs populated; boundary (`ev.epoch == current - LOOKBACK`) admits via strict `<`; within-window admits; pre-lookback network (current_epoch == LOOKBACK + genesis current_epoch == 0) admits all; multi-spend bundle short-circuits on FIRST expired evidence, error carries its epoch (not a later still-ok one). Test file: `tests/dsl_106_evidence_mempool_expired_rejected_test.rs`. |
| [DSL-107](NORMATIVE.md#DSL-107) | ✅ | Mempool duplicate rejected | 4 tests against new `enforce_slashing_evidence_mempool_dedup_policy(pending: &[SlashingEvidence], incoming: &[SlashingEvidence])` in `src/remark/policy.rs`. Fingerprint = `serde_json::to_vec(&ev)` bytes (same as DSL-102 wire format — no extra hash work). HashSet pre-filled from pending, then walk incoming: first collision → new `SlashingError::DuplicateEvidence` variant. 4 tests: pending/incoming collision rejects, in-batch incoming/incoming collision rejects, fully-distinct pending+incoming admits, empty inputs admit vacuously. Separate from DSL-026 manager-level AlreadySlashed (operates on evidence.hash, runs at manager layer); this is strictly upstream mempool dedup. Test file: `tests/dsl_107_evidence_mempool_duplicate_rejected_test.rs`. |
| [DSL-108](NORMATIVE.md#DSL-108) | ✅ | Block cap (> MAX_SLASH_PROPOSALS_PER_BLOCK) | 3 tests against new `enforce_block_level_slashing_caps(evidences: &[SlashingEvidence])` in `src/remark/policy.rs`. Strict `>` comparison: `evidences.len() > MAX_SLASH_PROPOSALS_PER_BLOCK` → new `SlashingError::BlockCapExceeded { actual, limit }`. Adds `MAX_SLASH_PROPOSALS_PER_BLOCK = 64` + `MAX_APPEALS_PER_BLOCK = 64` constants (appeal constant shipped early for DSL-119 reuse). 3 tests: MAX+1=65 rejects with both fields populated, exactly MAX admits (+ MAX-1 regression guard), empty admits vacuously. Test file: `tests/dsl_108_evidence_block_cap_test.rs`. |
| [DSL-109](NORMATIVE.md#DSL-109) | ✅ | Payload cap (> MAX_SLASH_PROPOSAL_PAYLOAD_BYTES) | 3 tests against new `enforce_slashing_evidence_payload_cap(evidences: &[SlashingEvidence])` in `src/remark/policy.rs`. Strict `>` on `serde_json::to_vec(ev).len()` → new `SlashingError::EvidencePayloadTooLarge { actual, limit }` variant (kept distinct from DSL-063 `AppealPayloadTooLarge` — different admission pipelines). Fixture uses `InvalidBlockProof::failure_witness: Vec<u8>` as the variable-size lever; binary-searches witness_len to hit cap boundary exactly. 3 tests: oversize rejects with both fields populated, at-limit (via tuner) admits, 1 KiB payload + empty list admit. Test file: `tests/dsl_109_evidence_payload_cap_test.rs`. Closes DSL-102..109 evidence-side REMARK block. |
| [DSL-110](NORMATIVE.md#DSL-110) | ✅ | Appeal wire roundtrip | 4 tests against new `src/remark/appeal_wire.rs`: `encode_slash_appeal_remark_payload_v1(&SlashAppeal)` emits `SLASH_APPEAL_REMARK_MAGIC_V1 \|\| serde_json(ap)`; `parse_slash_appeals_from_conditions(&[P: AsRef<[u8]>])` strips magic + decodes, silent-skip on short/foreign/malformed. Roundtrip PartialEq on full envelope including `SlashAppealPayload::Proposer(...)`; magic constant pinned to `b"DIG_SLASH_APPEAL_V1\0"`; cross-category isolation verified — an evidence-magic payload does NOT surface under the appeal parser; interleaved valid+foreign+valid preserves input order. Opens appeal-side REMARK block DSL-110..120. Test file: `tests/dsl_110_appeal_remark_wire_roundtrip_test.rs`. |
| [DSL-111](NORMATIVE.md#DSL-111) | ✅ | Appeal puzzle_reveal emits one REMARK | 4 tests against new `slash_appeal_remark_puzzle_reveal_v1` + `slash_appeal_remark_puzzle_hash_v1` in `src/remark/appeal_wire.rs`. Same constant-returning quoted puzzle shape `(q . ((1 payload)))` as DSL-103, different payload source (`SlashAppeal` JSON). Run in clvmr ChiaDialect(0) via `chia_sdk_types::run_puzzle`: output decodes as Vec<Condition<NodePtr>> of length 1 matching Condition::Remark; REMARK payload extracted via allocator.sexp pair-walk round-trips through DSL-110 parser back to original appeal; hash stable across repeated calls + changes when `appellant_index` mutates; hash equals `tree_hash_from_bytes(reveal)`. Test file: `tests/dsl_111_appeal_puzzle_reveal_emits_one_remark_test.rs`. |
| [DSL-112](NORMATIVE.md#DSL-112) | ✅ | Appeal admission matching coin | 4 tests against new `enforce_slash_appeal_remark_admission(&SpendBundle, &HashMap<Bytes32, Vec<Vec<u8>>>)` in `src/remark/policy.rs`. Appeal-side analogue of DSL-104: for each coin_spend, look up REMARK payloads, parse via DSL-110, derive DSL-111 puzzle hash, compare with `coin.puzzle_hash`. Shared `SlashingError::AdmissionPuzzleHashMismatch` variant (DSL-113 fail path). 4 tests: single matching spend admits, two-spend bundle both-match admits, empty bundle admits vacuously, spend absent from conditions map admits. Test file: `tests/dsl_112_appeal_admission_matching_coin_test.rs`. |
| [DSL-113](NORMATIVE.md#DSL-113) | ✅ | Appeal admission mismatch rejected | 3 tests against the DSL-112 `enforce_slash_appeal_remark_admission` fail path. Wrong `coin.puzzle_hash` → `SlashingError::AdmissionPuzzleHashMismatch { expected, got }` (shared variant with DSL-105); both fields populated with derived DSL-111 hash + coin ph; 3-spend (match, mismatch_b, mismatch_c) bundle proves short-circuit by asserting `got == wrong_b` and `got != wrong_c`. Test file: `tests/dsl_113_appeal_admission_mismatch_rejected_test.rs`. |
| [DSL-114](NORMATIVE.md#DSL-114) | ✅ | Appeal unknown slash rejected | 4 tests against new `enforce_slash_appeal_mempool_policy(appeals: &[SlashAppeal], pending_slashes: &HashSet<Bytes32>)` in `src/remark/policy.rs`. Returns `SlashingError::UnknownEvidence(hex)` when any appeal's `evidence_hash` is absent from the set. Reuses existing DSL-055 variant for unified diagnostic string — both layers emit 64-char lowercase hex via new `hex_encode_lower` helper. 4 tests: unknown rejects with hex field (verified lowercase 64-char), known admits, empty list vacuous, first-unknown short-circuits (`got` == first bad hex). Test file: `tests/dsl_114_appeal_mempool_unknown_slash_test.rs`. |
| [DSL-115](NORMATIVE.md#DSL-115) | ✅ | Appeal window expired | 5 tests against new `enforce_slash_appeal_window_policy(appeals: &[SlashAppeal], submitted_at: &HashMap<Bytes32, u64>)` in `src/remark/policy.rs`. Predicate `filed_epoch > submitted + SLASH_APPEAL_WINDOW_EPOCHS` → reuses existing `SlashingError::AppealWindowExpired { submitted_at, window, current }` (DSL-056 variant) for unified mempool/manager diagnostic shape. Unknown-hash appeals skip — DSL-114 handles that case. 5 tests: past-window rejects with all three fields, boundary admits (strict `>`), within-window admits, unknown-hash skipped, first-expired short-circuits carrying first bad entry's fields. Test file: `tests/dsl_115_appeal_mempool_window_expired_test.rs`. |
| [DSL-116](NORMATIVE.md#DSL-116) | ✅ | Appeal for finalised slash | 5 tests against new `enforce_slash_appeal_terminal_status_policy(appeals: &[SlashAppeal], status_by_hash: &HashMap<Bytes32, PendingSlashStatus>)` in `src/remark/policy.rs`. Reuses existing `SlashingError::SlashAlreadyFinalised` (DSL-061) + `SlashingError::SlashAlreadyReverted` (DSL-060) variants rather than a combined `AppealForFinalisedSlash(hex)` — keeps error surface narrow + caller can distinguish Finalised vs Reverted without string matching. Absent hash skips (DSL-114 owns that). 5 tests: Finalised rejects with DSL-061 variant, Reverted rejects with DSL-060 variant, Accepted admits, ChallengeOpen admits, unknown-hash skipped. Test file: `tests/dsl_116_appeal_mempool_finalised_slash_test.rs`. |
| [DSL-117](NORMATIVE.md#DSL-117) | ✅ | Appeal variant mismatch | 5 tests against new `enforce_slash_appeal_variant_policy(appeals, evidence_payload_by_hash: &HashMap<Bytes32, SlashingEvidencePayload>)` in `src/remark/policy.rs`. Valid pairings: P↔P, A↔A, I↔I; any cross-pairing → `SlashingError::AppealVariantMismatch` (DSL-057 variant reuse). Absent hash skips (DSL-114). 5 tests: P-vs-A rejects, A-vs-I rejects, I-vs-P rejects, all three matching-pair admits verified by exhaustion, unknown-hash skipped. Test file: `tests/dsl_117_appeal_mempool_variant_mismatch_test.rs`. |
| [DSL-118](NORMATIVE.md#DSL-118) | ❌ | Appeal duplicate rejected | 3 tests: pending-dup, incoming-dup, distinct. |
| [DSL-119](NORMATIVE.md#DSL-119) | ❌ | Appeal block cap | 2 tests: 65 rejected, 64 admits. |
| [DSL-120](NORMATIVE.md#DSL-120) | ❌ | Appeal payload cap | 2 tests: oversized rejected, at-limit admits. |

**Status legend:** ✅ verified · ⚠️ partial · ❌ gap
