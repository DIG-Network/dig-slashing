# DSL-164: AppealAdjudicationResult Serde Roundtrip

> **Authoritative:** [DSL-164](../NORMATIVE.md#DSL-164) | **Verification:** [VERIFICATION.md](../VERIFICATION.md) | **Tracking:** [TRACKING.yaml](../TRACKING.yaml) | **SPEC:** §3.9, §18

---

## Summary

`AppealAdjudicationResult` MUST round-trip byte-exactly via bincode + serde_json. Required for audit logs, RPC responses, and test fixture construction. Carries per-validator reverted stake/collateral vecs + bond + award + penalty fields.

---

## Specification

```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct AppealAdjudicationResult {
    pub appeal_hash: Bytes32,
    pub evidence_hash: Bytes32,
    pub outcome: AppealOutcome,
    pub reverted_stake_mojos: Vec<(u32, u64)>,
    pub reverted_collateral_mojos: Vec<(u32, u64)>,
    pub clawback_shortfall: u64,
    pub reporter_bond_forfeited: u64,
    pub appellant_award_mojos: u64,
    pub reporter_penalty_mojos: u64,
    pub appellant_bond_forfeited: u64,
    pub reporter_award_mojos: u64,
    pub burn_amount: u64,
}
```

---

## Acceptance Criteria

- [ ] Sustained result round-trips (reverted_* populated, appellant_award > 0).
- [ ] Rejected result round-trips (reporter_award > 0, reverted_* empty).
- [ ] `AppealOutcome::Won/Lost{reason_hash}/Pending` all round-trip.
- [ ] Vec fields preserve ordering.

---

## Implementation Notes

- **Primary file:** `src/result.rs`

---

## Verification

### Test Plan

| Test | Type | Description | Expected |
|------|------|-------------|----------|
| `test_dsl_164_sustained_bincode` | Unit | Sustained result | Equal |
| `test_dsl_164_rejected_bincode` | Unit | Rejected result | Equal |
| `test_dsl_164_json_roundtrip` | Unit | serde_json both outcomes | Equal |
| `test_dsl_164_outcome_variants` | Unit | Won / Lost / Pending | All Equal |
| `test_dsl_164_vec_ordering` | Unit | Multi-validator reverted_stake_mojos | Order preserved |

### Expected Test Files
- `tests/dsl_164_appeal_adjudication_result_serde_test.rs`

---

## Source Citations

- SPEC §3.9, §18

---

## References

- [DSL-068](DSL-068.md), [DSL-071](DSL-071.md) — adjudicator populates this
