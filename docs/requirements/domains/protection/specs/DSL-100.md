# DSL-100: Legacy JSON Loads With hash = None

> **Authoritative:** [DSL-100](../NORMATIVE.md#DSL-100) | **Verification:** [VERIFICATION.md](../VERIFICATION.md) | **Tracking:** [TRACKING.yaml](../TRACKING.yaml) | **SPEC:** §14.4, §22.11

---

## Summary

Legacy `SlashingProtection` JSON files (from an older schema version that predates `last_attested_block_hash`) MUST load successfully. Missing field defaults to `None` via `#[serde(default)]`. After one `record_attestation`, the file upgrades transparently.

---

## Specification

```rust
#[derive(Debug, Default, Clone, Serialize, Deserialize, PartialEq, Eq)]
pub struct SlashingProtection {
    pub last_proposed_slot: u64,
    pub last_attested_source_epoch: u64,
    pub last_attested_target_epoch: u64,
    #[serde(default)]
    pub last_attested_block_hash: Option<String>,
    pub last_attested_height: u64,
    pub last_attested_epoch: u64,
}
```

---

## Acceptance Criteria

- [ ] Legacy JSON without `last_attested_block_hash` field → loads with `None`.
- [ ] No error on missing field.
- [ ] Subsequent `save` emits the field.

---

## Implementation Notes

- **Primary file:** `src/protection.rs`

---

## Verification

### Test Plan

| Test | Type | Description | Expected |
|------|------|-------------|----------|
| `test_dsl_100_legacy_json_loads` | Unit | Load JSON without hash field | Ok, hash=None |
| `test_dsl_100_upgrade_on_save` | Unit | Load legacy, record_attestation, save | New JSON contains the hash field |

### Expected Test Files
- `tests/dsl_100_protection_legacy_json_test.rs`

---

## Source Citations

- SPEC §14.4, §22.11

---

## References

- [DSL-101](DSL-101.md) — save/load roundtrip
