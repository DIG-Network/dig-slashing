# DSL-094: check_proposal_slot Monotonic

> **Authoritative:** [DSL-094](../NORMATIVE.md#DSL-094) | **Verification:** [VERIFICATION.md](../VERIFICATION.md) | **Tracking:** [TRACKING.yaml](../TRACKING.yaml) | **SPEC:** §14.1, §22.11

---

## Summary

`check_proposal_slot(slot)` returns `true` iff `slot > last_proposed_slot`. After `record_proposal(slot)`, the same or lower slot fails. Prevents a validator from self-equivocating on restart.

---

## Specification

```rust
pub fn check_proposal_slot(&self, slot: u64) -> bool {
    slot > self.last_proposed_slot
}

pub fn record_proposal(&mut self, slot: u64) {
    self.last_proposed_slot = slot;
}
```

---

## Acceptance Criteria

- [ ] First call → true (default `last_proposed_slot = 0`, any slot > 0 ok).
- [ ] After record, same slot → false.
- [ ] After record, lower slot → false.
- [ ] After record, higher slot → true.

---

## Implementation Notes

- **Primary file:** `src/protection.rs`

---

## Verification

### Test Plan

| Test | Type | Description | Expected |
|------|------|-------------|----------|
| `test_dsl_094_first_ok` | Unit | check(10) on default | true |
| `test_dsl_094_same_after_record_false` | Unit | record(10), check(10) | false |
| `test_dsl_094_lower_after_record_false` | Unit | record(10), check(9) | false |
| `test_dsl_094_higher_after_record_true` | Unit | record(10), check(11) | true |

### Expected Test Files
- `tests/dsl_094_protection_proposal_monotonic_test.rs`

---

## Source Citations

- SPEC §14.1, §22.11

---

## References

- [DSL-097](DSL-097.md) — record + persistence
