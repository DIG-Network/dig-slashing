# Traits — Verification

| ID | Status | Summary | Verification Approach |
|----|--------|---------|----------------------|
| [DSL-131](NORMATIVE.md#DSL-131) | ✅ | ValidatorEntry::slash_absolute saturation | 5 tests pin the `ValidatorEntry::slash_absolute(amount, epoch)` contract via reference `MockValidator`. Opens Phase 9 External-State Traits. Semantics: `debited = min(amount, stake); stake -= debited; status = Slashed{epoch}; return debited`. Saturation is load-bearing — DSL-022 + DSL-030 debits can exceed remaining stake on partially-slashed validators. 5 tests: under-stake exact, at-stake zeroes balance, over-stake saturates (including `u64::MAX` vs 1 mojo), status flips on any call including zero-amount (DSL-162 compat), repeated debits compose with epoch updating to latest call. Test file: `tests/dsl_131_validator_entry_slash_absolute_saturation_test.rs`. |
| [DSL-132](NORMATIVE.md#DSL-132) | ✅ | ValidatorEntry::credit_stake | 4 tests pin `credit_stake(amount)` via reference MockValidator. Semantics: `delta = min(amount, u64::MAX - stake); stake += delta; return delta`. Return carries TRUE delta (not requested amount) so callers reconciling pre/post balance see consistent math — distinguishes from DSL-131 mock which returned requested amount. 4 tests: zero-stake credit(50) → stake=50/return=50; inverse of slash (slash(30)+credit(30) restores 100); saturation at u64::MAX with follow-up no-op; zero-amount no-op. Test file: `tests/dsl_132_validator_entry_credit_stake_test.rs`. |
| [DSL-133](NORMATIVE.md#DSL-133) | ✅ | ValidatorEntry::restore_status | 4 tests pin `restore_status()` via reference MockValidator. Returns `true` iff state flipped; `false` on no-op. Consumers: DSL-066 + DSL-129 (both may call on already-restored validator). 4 tests: slashed→active returns true + is_slashed flips, never-slashed no-op returns false, repeated restore (first true, subsequent false), re-slash after restore still takes effect (prevents Active-latching bug). Test file: `tests/dsl_133_validator_entry_restore_status_test.rs`. |
| [DSL-134](NORMATIVE.md#DSL-134) | ✅ | ValidatorEntry::is_active_at_epoch | 6 tests pin `activation_epoch <= e < exit_epoch` semantics via reference MockValidator. Left-inclusive, right-exclusive (Ethereum parity). Consumers: DSL-013/018 active-check + DSL-039 ValidatorNotActiveAtEpoch appeal. 6 tests: at-activation active, pre-activation inactive, exit-1 active, at-exit inactive, interior active (off-by-one inversion guard), exit==u64::MAX active through u64::MAX-1. Test file: `tests/dsl_134_validator_entry_is_active_boundary_test.rs`. |
| [DSL-135](NORMATIVE.md#DSL-135) | ✅ | ValidatorEntry::schedule_exit | 4 tests pin `schedule_exit(epoch)` via reference MockValidator. Unconditional overwrite — last call wins, including backwards moves (DSL-064/066 sustained-appeal revert rewinds exit lock to u64::MAX). Downstream `is_active_at_epoch` observes the new exit epoch post-call (right-exclusive check fires at the scheduled epoch). 4 tests: persists 110, multiple writes last-wins, downstream lifecycle check propagates, backward rewind + rewind-to-u64::MAX. Test file: `tests/dsl_135_validator_entry_schedule_exit_test.rs`. |
| [DSL-136](NORMATIVE.md#DSL-136) | ✅ | ValidatorView::get / get_mut | 5 tests pin `ValidatorView` surface via reference MockValidatorSet. Live indices Some, `len`/`len+1`/`u32::MAX` None, `get_mut` + mutation visible via next `get` (proves no clone/copy), `len` consistent across [0,1,5,100], empty set returns None for every index. Test file: `tests/dsl_136_validator_view_get_contract_test.rs`. |
| [DSL-137](NORMATIVE.md#DSL-137) | ✅ | EffectiveBalanceView::get / total_active | 4 tests pin `EffectiveBalanceView` via reference MockBalances. `get(idx)` returns balance or 0 (defensive default — consumers avoid branch-on-missing), `total_active()` == sum of per-validator gets. Consumers: DSL-081 base_reward, DSL-030 correlation, DSL-092 inactivity. 4 tests: distinct per-validator get, unknown + empty set → 0 + u32::MAX stress, total_active == manual sum, read-only invariance. Test file: `tests/dsl_137_effective_balance_view_test.rs`. |
| [DSL-138](NORMATIVE.md#DSL-138) | ✅ | PublicKeyLookup::pubkey_of | 3 tests pin new blanket `impl<T: ValidatorView + ?Sized> PublicKeyLookup for T` in `src/traits.rs`. Delegates `pubkey_of(idx)` → `self.get(idx).map(ValidatorEntry::public_key)`. Lets DSL-006/013 verifiers accept one trait-object pointer covering state-read + pubkey surfaces. 3 tests: direct vs blanket key identity matches, unknown idx + u32::MAX None, `&dyn PublicKeyLookup` function param accepts a ValidatorView impl and returns correct per-seed key. Test file: `tests/dsl_138_public_key_lookup_test.rs`. |
| [DSL-139](NORMATIVE.md#DSL-139) | ✅ | CollateralSlasher symmetry | 4 tests pin slash/credit contract. Extends trait with `fn slash(idx, amount, epoch) -> Result<(u64, u64), CollateralError>` — DEFAULT impl returns `Err(NoCollateral)` so existing `impl CollateralSlasher` fixtures (DSL-065/129/130) compile without change. New `CollateralError { NoCollateral, Disabled }` enum. 4 tests: slash/credit roundtrip restores (FullCollateral impl), NoCollateral soft-err state-unchanged, double-credit accumulates (fail-safe), default-impl NoCollateral verified via CreditOnlyCollateral omitting `slash`. Test file: `tests/dsl_139_collateral_slasher_symmetry_test.rs`. |
| [DSL-140](NORMATIVE.md#DSL-140) | ✅ | BondEscrow::escrowed | 5 tests pin read-side `escrowed(idx, tag) -> u64` via reference MockBondEscrow. Consumers: DSL-023 lock verification, DSL-068/071 pre-forfeit inspection, DSL-121..123 state observers. 5 tests: post-lock returns amount, post-release 0, post-forfeit 0 (parallel drain path), unknown (idx, tag) + u32::MAX + cross-tag-same-principal 0, read-only invariance across 10 repeats. Test file: `tests/dsl_140_bond_escrowed_query_test.rs`. |
| [DSL-141](NORMATIVE.md#DSL-141) | ✅ | RewardPayout::pay | 4 tests pin `pay(ph, amount)` via reference MockPayout (HashMap-backed per-address accumulator). Consumers: DSL-025 (whistleblower + proposer rewards), DSL-068/071 (winner-award bond split). 4 tests: single pay recorded, repeats accumulate (100+50+25=175), zero-amount tolerated + follow-up non-zero works, multi-principal isolation (3 distinct ph balances unaffected by each other). Test file: `tests/dsl_141_reward_payout_pay_test.rs`. |
| [DSL-142](NORMATIVE.md#DSL-142) | ✅ | RewardClawback::claw_back partial | 5 tests pin `claw_back(ph, amount) -> u64` via reference MockClawback. Returns ACTUAL clawed (saturating at balance), critical for DSL-067 sustained-appeal clawback and DSL-073 shortfall absorption from forfeited reporter bond. 5 tests: full balance claws requested, balance<amount returns exact balance (partial), empty balance 0, saturating at u64::MAX-5 + balance=5 with claw(u64::MAX), multiple clawbacks compose (first sees 100, second sees 40, third sees 0). Test file: `tests/dsl_142_reward_clawback_partial_test.rs`. |
| [DSL-143](NORMATIVE.md#DSL-143) | ❌ | JustificationView contract | 5 tests: each method callable; returns structured data; canonical_target_root None for uncommitted. |
| [DSL-144](NORMATIVE.md#DSL-144) | ❌ | ProposerView::proposer_at_slot | 3 tests: committed slot Some; future slot None; current_slot monotonic. |
| [DSL-145](NORMATIVE.md#DSL-145) | ❌ | InvalidBlockOracle::re_execute determinism | 3 tests: same inputs same outcome; Valid vs Invalid cases; no hidden state. |

**Status legend:** ✅ verified · ⚠️ partial · ❌ gap
