# Orchestration — Verification

| ID | Status | Summary | Verification Approach |
|----|--------|---------|----------------------|
| [DSL-127](NORMATIVE.md#DSL-127) | ✅ | run_epoch_boundary fixed ordering | 5 tests against new `src/orchestration.rs`. Opens Phase 8. `pub fn run_epoch_boundary` drives the 8-step pipeline: flag_deltas → inactivity update → penalties → finalise → rotate participation → advance manager → resize → prune. New `JustificationView` trait (DSL-143 preview, single `latest_finalized_epoch` method) + `EpochBoundaryReport` struct. Adds `SlashingManager::prune_processed_older_than(cutoff)` returning count; also sweeps `slashed_in_window` range older than cutoff. 5 tests use outcome-based ordering (full fake impls of `ValidatorView`/`EffectiveBalanceView`/`BondEscrow`/`RewardPayout`/`JustificationView`): end-to-end report population + epoch counters advanced; inactivity score `update_for_epoch` runs before finalise (stall triggers +=4 score on miss); prune is last (cutoff uses post-advance state); validator-count mismatch triggers resize to match; stall state propagated per `justification.latest_finalized_epoch`. Test file: `tests/dsl_127_epoch_boundary_order_test.rs`. |
| [DSL-128](NORMATIVE.md#DSL-128) | ✅ | SlashingSystem::genesis | 5 tests pin the at-birth aggregate state via new `src/system.rs`. Adds `GenesisParameters` (genesis_epoch + initial_validator_count + network_id) + `SlashingSystem` (manager + participation + inactivity) + `SlashingSystem::genesis`. Asserts: manager processed + book empty, trackers sized to initial_validator_count (7 + 5 fixtures), all flags + inactivity scores zero, epoch matches params (canonical 0 AND fork 1_234), `in_finality_stall(0, 0) == false`. Test file: `tests/dsl_128_genesis_initialisation_test.rs`. |
| [DSL-129](NORMATIVE.md#DSL-129) | ✅ | SlashingManager::rewind_on_reorg | 6 tests against new `SlashingManager::rewind_on_reorg(new_tip_epoch, &mut dyn ValidatorView, Option<&mut dyn CollateralSlasher>, &mut dyn BondEscrow) -> Vec<Bytes32>` in `src/manager.rs`. Adds `PendingSlashBook::submitted_after(epoch)` helper + `mark_processed` / `mark_slashed_in_window` / `is_slashed_in_window` pub methods for persistence load + tests. 6 tests use FakeValidator/SpyBondEscrow/SpyCollateral spies: credit_stake called with base_slash_amount; CollateralSlasher::credit called per validator when supplied; bond observed as `release` NOT `forfeit` at `BondTag::Reporter(hash)`; processed map + slashed_in_window row cleared post-rewind; reporter validator stake unchanged + restore_status not called (distinct from DSL-069); returns exactly the post-tip hashes, pre-tip slashes untouched. Test file: `tests/dsl_129_manager_rewind_on_reorg_test.rs`. |
| [DSL-130](NORMATIVE.md#DSL-130) | ✅ | rewind_all_on_reorg + ReorgTooDeep | 4 tests against new `pub fn rewind_all_on_reorg` + `ReorgReport` in `src/orchestration.rs`. Closes Phase 8. Adds new `SlashingError::ReorgTooDeep { depth, limit }` + `ParticipationTracker::rewind_on_reorg` (zero-fills both flag vectors + returns dropped epoch count) + `InactivityScoreTracker::rewind_on_reorg` (zero-fills all scores; accepts depth passthrough). Fixed 4-step order: manager (DSL-129) → participation → inactivity → protection (DSL-099). Strict `>` depth check vs `CORRELATION_WINDOW_EPOCHS`=36: depth==36 admits, depth==37 rejects with `{depth, limit}` populated + zero state mutation (scores preserved, manager epoch untouched). On success anchors `manager.current_epoch() = new_tip_epoch` and emits `protection_rewound: true`. 4 tests: all-four-rewound (protection hash cleared + scores zeroed + epoch anchored), depth=37 rejects no-mutation, boundary-36 admits, report populated with depth counters. Test file: `tests/dsl_130_rewind_all_on_reorg_test.rs`. |

| [DSL-165](NORMATIVE.md#DSL-165) | ✅ | EpochBoundaryReport + ReorgReport + FlagDelta serde | 6 tests pin byte-exact serde roundtrip for orchestration result types. Adds `Serialize, Deserialize` derives to EpochBoundaryReport + ReorgReport in src/orchestration.rs (FlagDelta already had them). 6 tests: populated EpochBoundaryReport bincode (3 flag_deltas + 2 inactivity_penalties + 2 finalisations with nested correlation_penalty vecs — spot-checks decode at every nesting level), populated ReorgReport bincode (3 distinctive Bytes32 rewound hashes + epochs_dropped + protection_rewound flag, order-preservation asserts), standalone FlagDelta bincode with u32::MAX + u64::MAX + zero extremes, all three under serde_json PLUS SlashingResult cross-check (RPC batches both), empty-vec cases for both reports survive both codecs (dashboards rely on stable struct shape), in_finality_stall + protection_rewound true/false under both codecs (guards against bool flip that would misattribute DSL-091 vs DSL-092 penalty branches). Test file: `tests/dsl_165_epoch_boundary_reorg_report_serde_test.rs`. |

| [DSL-169](NORMATIVE.md#DSL-169) | ❌ | run_epoch_boundary wires reward_payout + inactivity penalty slashes | 5 tests: FlagDelta reward paid, zero-reward no pay, inactivity penalty slashes validator stake, missing validator skipped, step order preserved. |
| [DSL-170](NORMATIVE.md#DSL-170) | ❌ | SlashingSystem carries network_id | 4 tests: genesis stores id, accessor returns reference, distinct ids, GenesisParameters shape unchanged. |

**Status legend:** ✅ verified · ⚠️ partial · ❌ gap
