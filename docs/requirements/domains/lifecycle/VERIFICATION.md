# Optimistic Slashing Lifecycle — Verification

| ID | Status | Summary | Verification Approach |
|----|--------|---------|----------------------|
| [DSL-022](NORMATIVE.md#DSL-022) | ✅ | submit_evidence base_slash = max(bps, quotient) | 10 tests: bps dominates proposer (eff/20 wins at 500 bps), floor dominates attester double + surround (eff/32 beats eff/100), invalid-block floor wins integer-rounding tiebreak, per-validator vec cardinality equals intersection, already-slashed skipped, absent validator → verify-error, `eff_bal=0` yields zero slash, determinism, `slash_absolute` called with `current_epoch`. Test file: `tests/dsl_022_submit_evidence_base_slash_formula_test.rs`. |
| [DSL-023](NORMATIVE.md#DSL-023) | ✅ | submit_evidence escrows REPORTER_BOND_MOJOS via BondEscrow::lock | 8 tests: `lock(reporter_idx, REPORTER_BOND_MOJOS, Reporter(hash))` invoked once with exact args, lock precedes `slash_absolute` (shared-clock ordering), `SlashingResult::reporter_bond_escrowed == REPORTER_BOND_MOJOS`, tag carries `evidence.hash()`, `InsufficientBalance → BondLockFailed` with no `slash_absolute`, `REPORTER_BOND_MOJOS == MIN_EFFECTIVE_BALANCE / 64` constant check, determinism across managers, `OffenseTooOld` surfaces before the lock attempt. Test file: `tests/dsl_023_submit_evidence_escrows_reporter_bond_test.rs`. |
| [DSL-024](NORMATIVE.md#DSL-024) | ✅ | submit_evidence inserts PendingSlash { Accepted, window_expires } | 9 tests: inserted record has `status == Accepted`, `submitted_at_epoch == current_epoch` + `window_expires_at_epoch == current_epoch + SLASH_APPEAL_WINDOW_EPOCHS (8)`, per-validator vec cardinality + equality with `SlashingResult::per_validator`, `reporter_bond_mojos == REPORTER_BOND_MOJOS`, `appeal_history.is_empty()`, `processed[hash] == current_epoch`, `SlashingResult::pending_slash_hash == evidence.hash()`, determinism across managers, `book.len()` grows by 1. Test file: `tests/dsl_024_submit_evidence_creates_pending_accepted_test.rs`. |
| [DSL-025](NORMATIVE.md#DSL-025) | ✅ | submit_evidence routes wb_reward + prop_reward | 9 tests: happy-path amounts (`wb=total_eff/512`, `prop=wb/8`), reporter puzzle hash target, block-proposer puzzle hash target, `burn_amount == total_base - wb - prop` accounting, multi-validator totals (3 signers), zero rewards when `eff_bal < 512` still emits two `pay(_, 0)` calls, `proposer_at_slot(current_slot())` recorded exactly once, determinism across managers, `ProposerUnavailable` error surface (reporter reward already paid, documents ordering). Test file: `tests/dsl_025_submit_evidence_reward_routing_test.rs`. |
| [DSL-026](NORMATIVE.md#DSL-026) | ✅ | submit_evidence rejects AlreadySlashed on duplicate | 7 tests: duplicate submit → `AlreadySlashed`, no state mutation (bond / reward / slash / book counts unchanged), dedup precedes verify (corrupted dup still surfaces `AlreadySlashed`), `is_processed(hash)` helper, distinct evidences admitted independently, duplicate path makes no `bond.lock` call, duplicate path makes no `reward.pay` call. Test file: `tests/dsl_026_submit_evidence_already_slashed_test.rs`. |
| [DSL-027](NORMATIVE.md#DSL-027) | ✅ | submit_evidence rejects PendingBookFull at capacity | 7 tests (with `SlashingManager::with_book_capacity(n)` tiny-capacity harness): submit succeeds at `capacity - 1`, rejected at `capacity` with `PendingBookFull`, no `bond.lock`, no `slash_absolute`, no `processed` entry (retry permitted), no `reward.pay`, verify runs before capacity check (`OffenseTooOld` surfaces at capacity). Test file: `tests/dsl_027_submit_evidence_book_full_test.rs`. |
| [DSL-028](NORMATIVE.md#DSL-028) | ✅ | submit_evidence rejects BondLockFailed on insufficient stake | 8 tests with `ToggleBond` mock: `InsufficientBalance → BondLockFailed`, no `slash_absolute`, no `reward.pay`, no pending insert, `processed` NOT set (retry permitted), retry after flipping mock to accepting succeeds, error variant exact, `DoubleLock` also maps to `BondLockFailed` (coarse mapping). Test file: `tests/dsl_028_submit_evidence_bond_lock_failed_test.rs`. |
| [DSL-029](NORMATIVE.md#DSL-029) | ✅ | finalise transitions Accepted/ChallengeOpen to Finalised | 7 tests: Accepted→Finalised at `window + 1`, ChallengeOpen covered by same match arm, `FinalisationResult::evidence_hash` matches, idempotent (second call empty), empty-book returns empty, multiple admissions deterministic order, not-yet-expired untouched (stays Accepted). Test file: `tests/dsl_029_finalise_transitions_to_finalised_test.rs`. |
| [DSL-030](NORMATIVE.md#DSL-030) | ✅ | finalise applies correlation penalty | 8 tests: isolated-slash small penalty, saturation at `eff_bal` when `cohort*3 >= total`, cohort-window bounds exclude pre-window admissions, multiplier = 3 (`cohort = total/6 → penalty = eff/2`), penalty recorded in `FinalisationResult` + `slash_absolute` called at finalise epoch, zero-cohort zero-penalty, `total_active = 0` zero-penalty, determinism across managers. Test file: `tests/dsl_030_finalise_applies_correlation_penalty_test.rs`. u128 intermediate prevents overflow in `eff_bal * capped`. |
| [DSL-031](NORMATIVE.md#DSL-031) | ✅ | finalise returns reporter bond in full | 7 tests with `RecordingBond`: `release(reporter_idx, REPORTER_BOND_MOJOS, Reporter(hash))` called once, `reporter_bond_returned == REPORTER_BOND_MOJOS`, lock amt == release amt, tag equals `evidence.hash()`, idempotent (second call no extra release), three distinct pendings yield three distinct release tags, release `principal_idx` equals reporter (not proposer). Test file: `tests/dsl_031_finalise_returns_reporter_bond_test.rs`. |
| [DSL-032](NORMATIVE.md#DSL-032) | ✅ | finalise schedules exit lock `current + SLASH_LOCK_EPOCHS` | 7 tests: Proposer single `schedule_exit(current + 100)` call, AttesterSlashing with 3 indices → 3 calls same lock, arithmetic (current=50 → lock=150), `SLASH_LOCK_EPOCHS == 100`, missing-from-view tolerated no-panic, determinism across managers, `FinalisationResult::exit_lock_until_epoch` matches call arg. Test file: `tests/dsl_032_finalise_schedules_exit_lock_test.rs`. |
| [DSL-033](NORMATIVE.md#DSL-033) | ✅ | finalise skips already-Reverted slashes | 8 tests: Reverted → empty result, no `slash_absolute`, no `bond.release`, no `schedule_exit`, idempotent (Finalised skipped on second call), mixed batch (Accepted+Reverted → only Accepted finalised), pending retained in book with Reverted status, determinism. Test file: `tests/dsl_033_finalise_skips_reverted_test.rs`. |
| [DSL-146](NORMATIVE.md#DSL-146) | ✅ | PendingSlashBook basic ops | 5 tests pin new/insert/get/remove/len + capacity contract. Opens Phase 10 Gap Fills. PendingSlashBook was shipped earlier as DSL-024 dependency; this test seals the CRUD surface so internal HashMap/BTreeMap refactor cannot drift observable behavior. 5 tests: new(10) empty + is_empty, insert increments + get returns Some, unknown hash None (also after insert of different hash), remove returns owned record + decrements len + post-remove None + double-remove None, insert at capacity returns `SlashingError::PendingBookFull` + state unchanged (DSL-027 interlock). Test file: `tests/dsl_146_pending_slash_book_basic_ops_test.rs`. |
| [DSL-147](NORMATIVE.md#DSL-147) | ❌ | PendingSlashBook::expired_by | 4 tests: expired returned, boundary excluded, Reverted excluded, ordered. |
| [DSL-148](NORMATIVE.md#DSL-148) | ❌ | SlashingManager::new + set_epoch | 4 tests: empty state, epoch preserved, set_epoch updates, distinct from genesis. |
| [DSL-149](NORMATIVE.md#DSL-149) | ❌ | SlashingManager::is_slashed | 3 tests: slashed true, active false, unknown false. |
| [DSL-150](NORMATIVE.md#DSL-150) | ❌ | is_processed + pending + prune | 6 tests. |
| [DSL-151](NORMATIVE.md#DSL-151) | ❌ | Correlation penalty saturation clamp | 5 tests: small, mass, exact ceiling, zero total, overflow. |
| [DSL-152](NORMATIVE.md#DSL-152) | ❌ | submit_evidence ReporterIsAccused short-circuit | 4 tests: proposer + attester self-accuse, no state mutation, distinct ok. |

| [DSL-162](NORMATIVE.md#DSL-162) | ❌ | submit_evidence skips already-slashed indices | 4 tests: single skipped, partial intersection, all indices slashed, no window insert. |
| [DSL-163](NORMATIVE.md#DSL-163) | ❌ | SlashingResult + PerValidatorSlash + FinalisationResult serde | 5 tests: bincode all types, json all types, zero preserved. |

**Status legend:** ✅ verified · ⚠️ partial · ❌ gap
